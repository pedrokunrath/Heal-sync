version: '3.2'
services:
  HealSync-mail:
    image: 'sj26/mailcatcher'
    ports:
      - "1080:1080"
      - "1025:1025"

  HealSync-redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  HealSync-db:
    image: postgres:14-alpine
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    volumes:
      - "HealSync-db-data:/var/lib/postgresql/data"

volumes:
  HealSync-db-data: